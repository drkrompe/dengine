import React from 'react';

export default (props) => {
    return (
        <div style={
            {
                width: '100%',
                backgroundColor: 'blue'
            }
        }>
            header
        </div>
    )
}